<h2>Available Waiters</h2>
<div class="card-group">

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Sunday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each sunWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>

      <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
    </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Monday</a>
<table class="table table-striped table-inverse table-responsive">
  <thead>

  </thead>
  <tbody>
    {{#each monWaiters}}
    <tr>
      <td class="text-center">{{this}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
    </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Tuesday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each tuesWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Wednesday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each wedWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Thursday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each thursWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
     </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Friday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each frWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
     </div>
  </div>

  <div class="card">
    <div class="card-block">
      <a href="#" class="btn btn-primary btn-block">Saturday</a>
      <table class="table table-striped table-inverse table-responsive">
        <thead>

        </thead>
        <tbody>
          {{#each satWaiters}}
          <tr>
            <td class="text-center">{{this}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

</div>
